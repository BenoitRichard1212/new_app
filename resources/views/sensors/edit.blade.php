@extends('sensors.layout')
   
@section('content')
    <div class="row">
        <div class="col-lg-12 margin-tb">
            <div class="pull-left">
                <h2>Edit Sensors</h2>
            </div>
            <div class="pull-right">
                <a class="btn btn-primary" href="{{ route('sensors.index') }}"> Back</a>
            </div>
        </div>
    </div>
   
    @if ($errors->any())
        <div class="alert alert-danger">
            <strong>Whoops!</strong> There were some problems with your input.<br><br>
            <ul>
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
  
    <form action="{{ route('sensors.update',$sensors->name) }}" method="POST">
        @csrf
        @method('PUT')
   
         <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Sensor:</strong>
                    <input type="text" name="sensor" value="{{ $sensors->sensor }}" class="form-control" placeholder="Sensor">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Temperature:</strong>
                    <textarea class="form-control" style="height:150px" name="temperature" placeholder="Temperature">{{ $sensors->temperature }}</textarea>
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Humidity:</strong>
                    <textarea class="form-control" style="height:150px" name="humidity" placeholder="Humidity">{{ $sensors->humidity }}</textarea>
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Device:</strong>
                    <textarea class="form-control" style="height:150px" name="device" placeholder="Device">{{ $sensors->device }}</textarea>
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12 text-center">
              <button type="submit" class="btn btn-primary">Submit</button>
            </div>
        </div>
   
    </form>
@endsection
